# My Company Brain·企业知识中台 PRD

状态：可运行产品定义

## 1. 产品目标

为企业提供一套可治理、可追溯、具备权限边界的知识应用中台。业务用户可以上传和使用知识、创建场景并在全域问答中获得带来源的结果；管理员负责账号、权限、知识链路和运行状态治理。

产品重点：

- 多条 RAG 链路如何接入统一应用层；
- 用户、组织、团队和知识可见范围如何贯穿检索；
- Agent 如何基于检索证据完成问答与任务；
- 多数据库服务如何通过 Docker Compose 统一运行。

## 2. 用户角色

### 业务成员

- 注册时选择可加入的团队，注册后直接使用系统。
- 使用个人、团队或公司范围内有权访问的知识。
- 创建业务场景、上传资料、发起问答并查看来源。
- 只能召回符合本人组织、团队和所有权边界的数据。

### 后台管理员

- 管理账号、团队权限和公司级知识发布。
- 管理三条 RAG 管线、知识库、图谱、评估和系统状态。
- 处理需要复核的资料与发布任务。

注册完成后直接进入产品；管理员权限仍由后台治理，不允许注册请求自行指定组织、团队集合或管理员身份。

## 3. 核心业务范围

### 3.1 身份与权限

- 登录、注册、会话与管理员身份。
- 注册页面展示可选团队，团队为选填项。
- 注册请求只接受团队 ID，不接受客户端指定组织、团队集合或管理员身份。
- 知识资产支持个人、团队、公司三种可见范围。
- 查询、场景、知识资产和全域问答按用户上下文执行权限过滤。

### 3.2 前台工作台

- 应用总览：查看可用能力和最近工作。
- 全域问答：组合检索、连续追问、来源证据与历史会话。
- 方案模板：从标准模板创建业务应用。
- 业务场景：管理个人、团队和公司级场景。
- 处理任务：查看资料处理和复核状态。
- 知识资产：查看本人有权访问的知识。
- 空间设置：聚焦当前账号、组织、团队与知识权限范围，不提供后台系统配置。

### 3.3 后台治理

- 用户、知识库、管线与发布策略。
- GraphRAG 图谱查看和维护。
- 评估、审计、监控、诊断与系统设置。
- 公司级知识和高权限能力由管理员治理。

### 3.4 三条知识链路

| 链路 | 定位 | 主要数据 |
|---|---|---|
| Nano Brain | 页面化知识、事实沉淀与知识导航 | 页面、事实、引用 |
| Traditional RAG | 文档切分、向量检索和来源证据 | 文档、分块、向量 |
| GraphRAG | 实体关系、多跳路径和图谱证据 | 文档、实体、关系 |

三条链路保持独立存储和实现，通过平台 API 与 Agent Gateway 统一接入，不把内部模型强行合并。

### 3.5 Agent 编排

- Agent 根据任务选择适合的知识链路。
- 检索结果作为生成回答的证据上下文。
- 全域问答展示文档证据与关系图谱来源。
- 对话历史、来源面板和主问答区支持适合阅读的布局。

## 4. 数据与部署

Docker Compose 运行以下核心服务：

- Web
- API
- Agent Gateway
- Nano Brain
- Traditional RAG
- GraphRAG
- PostgreSQL
- Neo4j
- 一次性数据库迁移服务

PostgreSQL 内使用 6 个独立业务数据库；GraphRAG 的关系图存储在 Neo4j。服务只通过内部网络访问数据库，默认仅 Web 端口映射到宿主机。

运行环境会在首次初始化时准备基础业务数据；检测到已有成员数据时禁止覆盖。

## 5. 验收标准

1. 配置本地 `.env` 后运行 `deploy/compose/start.sh`，即可完成构建和启动。
2. Web、API、Agent Gateway、三条 RAG、PostgreSQL 与 Neo4j 均能进入预期健康状态。
3. 管理员能使用当前环境配置的密码登录。
4. 已入库的业务场景、问答历史、知识文档、文件和图谱数据可被读取。
5. 业务成员不能越权访问其他个人或团队范围的数据。
6. 注册用户不能自行指定管理员身份或任意组织边界。
7. 再次运行启动脚本不会覆盖成员后续产生的数据。

## 6. 非目标

- 公网生产级高可用、自动扩缩容和容灾。
- 镜像 Registry 分发与远程代码仓库维护。
- 企业 SSO、复杂审批流和注册审批。
- 计费、配额和完整 SaaS 商业化能力。
- 在项目配置中内置任何真实 API Key。
