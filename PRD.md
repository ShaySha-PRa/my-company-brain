# My Company Brain·企业知识中台产品基线

状态证据集中在 [`docs/CURRENT_STATUS.md`](docs/CURRENT_STATUS.md)。本文描述当前产品范围、长期业务行为和发布门禁；“已实现”不自动等于“已真实环境验证”。

## 1. 产品目标

My Company Brain 为企业团队提供可治理、可追溯、具备权限边界的多知识库工作台。业务成员可以管理资料、创建业务场景、向知识助理提问并查看来源证据；管理员负责账号、组织、团队、知识链路、质量检查和运行状态。

产品由三条相互独立的知识链路组成：

- Nano Brain：知识页面、事实、链接和自治运行。
- Traditional RAG：文档与表格、分块、关键词/字面/向量三路召回及来源证据。
- GraphRAG：实体、关系、多跳路径、图谱问答和治理。

统一 API、平台业务层、Agent Gateway 和 Web 工作台提供一致的身份、权限、场景、任务、问答和审计体验。

## 2. 用户与权限

### 业务成员

- 注册时可以选择受控团队，不能从请求中指定组织集合或管理员身份。
- 使用个人、团队或公司范围内有权访问的知识。
- 创建业务场景、上传资料、发起问答、查看来源和跟进处理任务。
- 查询边界只允许 public、本人 private 以及本人所在团队交集中的数据。

### 后台管理员

- 管理用户、组织、团队、知识库、知识链路、场景模板和运行配置。
- 查看审计、诊断、监控、评估和知识资产，执行需要管理员权限的复核、发布和治理操作。
- 不能通过前端隐藏按钮替代服务端权限检查；模块查询边界必须重新验证权限。

## 3. 产品表面

### 成员工作台

- 应用总览、方案模板、业务场景、处理任务、知识资产和空间设置。
- 全域问答、连续追问、SSE 增量消息、对话历史和来源证据面板。
- 场景资料提交、处理状态、发布状态、产物和后续任务。

### 管理工作台

- 用户与权限、知识库和三条链路的资产管理。
- 文档处理、页面复核、图谱治理、召回验证、评估、审计、监控、诊断和系统设置。
- 复核型 Agent 可以读取和保存候选，但不能代替管理员执行批准、驳回或要求修改。

上述界面和服务入口已在代码中提供；逐路由浏览器流程和业务资料验收见当前状态页的待验证清单。

## 4. 服务与数据边界

| 服务 | 内部端口 | 职责 | 数据边界 |
|---|---:|---|---|
| Web | 3000 | 成员与管理员界面 | 只调用统一 API 和 Agent Gateway |
| API | 3101 | 身份鉴权、请求归一化、HTTP 分发 | 身份及平台边界由对应包处理，不直连知识模块数据库 |
| Agent Gateway | 3002 | Agent、MCP、SSE、会话和 checkpoint | `mcb_agent_db` |
| Nano Brain | 8100 | 知识页面链路 | `mcb_nano_db` |
| Traditional RAG | 8101 | 文档与表格知识链路 | `mcb_traditional_db` |
| GraphRAG | 8102 | 关系知识链路 | `mcb_graph_db`、Neo4j |
| PostgreSQL | 5432 | 六个业务数据库 | `mcb_identity_db`、`mcb_core_db`、`mcb_nano_db`、`mcb_traditional_db`、`mcb_graph_db`、`mcb_agent_db` |
| Neo4j | 7474/7687 | 图节点、关系和图查询 | 持久化数据与插件卷 |

Compose 项目使用 `mcb` 前缀和内部网络。宿主机端口可在私有 `.env` 中调整，不能据此改变服务内部端口约定。

## 5. 关键产品行为

- API 向模块转发时注入 `x-mcb-internal-token`、`x-mcb-user-id`、`x-mcb-username`、`x-mcb-is-admin`；模块验证令牌并在 SQL 查询边界执行权限过滤。
- Embedding 使用 MiniMax 原生请求格式，入库和查询分别使用 `type=db` 与 `type=query`；1536 维结果截断至 1024 维并做 L2 归一化。
- Traditional RAG 在一次请求内串行执行 tsvector、trigram/字面和向量召回，并用全候选集合的最大 RRF 做归一化。
- Nano Brain 组合页面关键词、页面向量、事实关键词、事实向量和链接扩展；已发布事实的变更必须经过复核。
- GraphRAG 使用 LightRAG 和 Neo4j，工作区、实体、关系和来源操作必须保持权限与可恢复一致。
- 全域问答在平台层合并三条链路结果，可选使用 qwen3-rerank；未配置可选重排时保留召回结果，不因缺少可选密钥中断。
- MiniMax M2.x 的 `<think>` 块在流式输出和结构化 JSON 校验前处理；不得用会破坏工具调用的禁用思考组合。

## 6. 部署与数据安全

使用 `deploy/compose/start.sh` 从私有 `deploy/compose/.env` 构建并启动。迁移服务成功退出后，依赖服务才会启动；健康检查覆盖进程和依赖可用性。数据库、文档文件、平台文件、图工作目录和 checkpoint 使用持久卷。

启动脚本拒绝模板密钥和过短内部令牌。密钥只从本地环境文件注入，不进入 Git、镜像层、日志或前端包。重置操作需要显式确认；恢复流程遇到非空目标应拒绝覆盖。

## 7. 当前证据与发布门禁

当前快照已经记录：命名扫描通过、Bun 88 项测试通过、TypeScript/Python 类型检查通过、Traditional Python 8/8 和 Graph Python 7/7 通过；九个 Compose 服务中八个持续服务健康，迁移服务退出码为 0，五个健康接口返回 HTTP 200。这些结果分别属于已自动验证或已真实环境验证，完整命令见 [`docs/CURRENT_STATUS.md`](docs/CURRENT_STATUS.md)。

以下项目仍必须取得对应证据后才能发布：

1. 三条链路使用自建资料完成真实入库、检索、回答和来源引用。
2. 真实 MiniMax embedding、Agent 工具调用、思考块处理、限速和降级。
3. 普通成员/管理员权限矩阵，包括个人、团队、公司可见范围和越权拒绝。
4. 浏览器全路由、登录、场景、问答、后台治理、响应式和可访问性。
5. Compose 重启后的数据库、文件、图数据、会话和 checkpoint 保持性。
6. `notebooks/` 验收文件的建立与运行；当前目录没有可执行 notebook。

产品整体在全部门禁有新鲜证据前，状态保持为待验证，不把静态代码存在或服务健康等同于完整发布验收。

## 8. 非目标

- 公网生产级高可用、自动扩缩容和跨区域容灾。
- 镜像 Registry 分发和远程仓库维护。
- 企业 SSO、复杂审批流和注册审批。
- 计费、配额和完整 SaaS 商业化能力。
- 在项目配置中内置任何真实 API Key。
