## 1. Build Reconciled Discovery Catalogs

- [x] 1.1 Enumerate all employee and administrator routes, page states, actions, navigation entries, and role redirects into a temporary web catalog with stable candidate identifiers.
- [x] 1.2 Enumerate all unified API, Agent Gateway, Nano Brain, Traditional RAG, and GraphRAG HTTP and MCP operations, including errors, authorization rules, and streaming events, into a temporary interface catalog.
- [x] 1.3 Enumerate all six PostgreSQL databases, schemas, tables, columns with behavioral significance, vector indexes, graph stores, roles, and minimum privileges into a temporary persistence catalog.
- [x] 1.4 Enumerate all ingestion, retrieval, curation, dream, fact-review, Agent, rerank, checkpoint, audit, deployment, restore, health, smoke, and maintenance behavior into a temporary capability catalog.
- [x] 1.5 Enumerate all runnable scripts, automated tests, browser checks, container checks, and notebook validation cells into a temporary verification catalog.
- [x] 1.6 Reconcile the five discovery catalogs so every item has one owning area, one candidate feature identifier, and no unexplained duplicate or unmatched entry.

## 2. Create the Complete Feature Inventory

- [x] 2.1 Create `docs/feature-inventory.md` with scope, counting rules, stable identifier conventions, module definitions, and a reproducible totals section.
- [x] 2.2 Add every employee-facing and administrator-facing page and workflow, including empty, loading, success, error, forbidden, and role-routing behavior.
- [x] 2.3 Add every platform, Agent, and module interface with method, path, behavior, owner, permission boundary, persistence mapping, and verification method.
- [x] 2.4 Add every database table and graph persistence area, including the six-database ownership map, `vector(1024)` and HNSW requirements, and minimum-privilege responsibilities.
- [x] 2.5 Add every script and operational workflow for initialization, migration, administrator creation, fixture preparation, ingestion, smoke verification, deployment, restore, status, diagnostics, and reset.
- [x] 2.6 Add the full Traditional RAG, GraphRAG, Nano Brain, global retrieval, Agent orchestration, citation, checkpoint, audit, and optional-provider degradation behaviors.
- [x] 2.7 Attach at least one executable or explicit manual verification method to every feature row and reconcile row totals with the discovery catalogs.

## 3. Create the Full Capability Coverage Ledger

- [x] 3.1 Create `docs/capability-coverage.md` with coverage statuses, owner-decision rules, and a one-to-one mapping to feature inventory identifiers.
- [x] 3.2 Document product structure and behavior for web, unified API, Agent Gateway, Nano Brain, Traditional RAG, GraphRAG, and the platform package layer.
- [x] 3.3 Document the six PostgreSQL databases, vector indexes, PostgreSQL graph support where applicable, Neo4j graph storage, roles, migrations, and minimum privileges.
- [x] 3.4 Document all three retrieval engines, platform-only global reranking, permissions, citations, conversation memory, MCP integration, and human approval gates.
- [x] 3.5 Document Docker Compose services, one-shot migration, networks, volumes, health checks, observation ports, restoration safeguards, and local development workflows.
- [x] 3.6 Add the complete `My Company Brain` and `mcb` rename matrix for visible copy, headers, environment variables, roles, images, networks, volumes, directories, scripts, fixtures, tests, notebooks, and sample data.
- [x] 3.7 Record any proposed product change only as an owner decision, then verify that every inventory identifier has exactly one coverage status.

## 4. Create the Technical Design

- [x] 4.1 Create `docs/design.md` with the complete monorepo tree and ownership boundaries for apps, modules, packages, deployment, scripts, tests, notebooks, and documentation.
- [x] 4.2 Define the six-database topology, key tables, `vector(1024)` columns, cosine HNSW indexes, relational/vector stores, and graph-store responsibilities.
- [x] 4.3 Define the MiniMax chat, Agent, native embedding, normalization, thinking-block removal, rate-limit tuning, rerank degradation, and PDF degradation contracts for TypeScript and Python runtimes.
- [x] 4.4 Define ports, `x-mcb-*` internal headers, product-owned environment naming, token handling, service networks, and the prohibition on direct module-database access by gateways and frontend code.
- [x] 4.5 Define retrieval algorithms and invariants, including serial three-path recall, RRF global normalization, Nano Brain multi-path retrieval, GraphRAG workspace isolation, and platform-only global reranking.
- [x] 4.6 Define Phases 1 through 8 with dependencies, deliverables, executable exit criteria, real-environment tests, and owner review gates.

## 5. Cut Over Repository Governance

- [x] 5.1 Confirm the three baseline documents are complete and internally consistent before replacing any migration-era governance.
- [x] 5.2 Rewrite `AGENTS.md` as the self-contained My Company Brain constitution, preserving every architecture, MiniMax, retrieval, permission, engineering, and acceptance rule while using only the `mcb` namespace.
- [x] 5.3 Rewrite `INITIAL-TASK.md` as the independent product development brief without unrelated context or superseded identifiers.
- [x] 5.4 Cross-check both rewritten governance files against the three baseline documents and account for every mandatory constraint.

## 6. Validate Phase 0 Outputs and Repository Hygiene

- [x] 6.1 Verify that only the three authorized `docs/` deliverables were created, only the two approved governance files were rewritten, and no business implementation file changed.
- [x] 6.2 Run structural checks for required sections, stable identifiers, required row fields, valid dispositions, and missing or duplicate mappings.
- [x] 6.3 Recalculate and verify all feature, module, page, route, table, script, and verification totals.
- [x] 6.4 Scan the complete project working tree, excluding only `.git` and out-of-scope files, and confirm zero disallowed naming or content matches outside the controlled audit configuration.
- [x] 6.5 Scan for placeholders, hidden omissions, unsupported completion claims, unresolved contradictions, and owner decisions incorrectly marked complete; correct every failure.
- [x] 6.6 Cross-check architecture, permission, model, retrieval, ports, database, governance, and acceptance statements across all five changed documents for one consistent contract.

## 7. Hand Off for Owner Review

- [x] 7.1 Report the three document paths, total feature count, covered module count, principal risks, governance-cutover status, and the decisions most important for the owner to confirm.
- [x] 7.2 Stop after the Phase 0 handoff and wait for explicit owner approval before beginning any Phase 1 implementation.
