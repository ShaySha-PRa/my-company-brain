# Browser Tests

Browser checks cover the current web interface: task-oriented entry points, loading/empty/success/error/forbidden states, responsive layout, and employee/administrator routing.

Current evidence status: 待验证. A successful TypeScript test run does not replace the Playwright checks; record a real browser run before claiming page-level acceptance.
